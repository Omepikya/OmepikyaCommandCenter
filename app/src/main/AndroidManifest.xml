<?xml version="1.0" encoding="utf-8"?>

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.omepikya.commandcenter">

    <!-- Voice commands -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Phone-book/contact lookup -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Required for boot-time automation restoration -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Android 13+ notification permission -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Exact alarms when the platform requires special access -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:fullBackupContent="@xml/backup_rules"
        android:theme="@style/AppTheme">

        <!-- Scheduled automation receiver is internal and is invoked through an explicit PendingIntent. -->
        <receiver
            android:name=".automation.AutomationReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Boot receiver must be explicit for Android 12+ and is invoked by the system. -->
        <receiver
            android:name=".automation.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">

            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>

        </receiver>

        <activity
            android:name=".HelpActivity"
            android:exported="false" />

        <activity
            android:name=".ProactiveSettingsActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>